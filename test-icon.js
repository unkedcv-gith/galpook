import { icons } from 'lucide-react';
console.log(Object.keys(icons).filter(name => name.toLowerCase().includes('face') || name.toLowerCase().includes('smile') || name.toLowerCase().includes('dizzy') || name.toLowerCase().includes('laugh') || name.toLowerCase().includes('ghost')));
